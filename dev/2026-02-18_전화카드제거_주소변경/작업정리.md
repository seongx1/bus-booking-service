# 2026-02-18 작업 정리: 전화 카드 제거 및 주소 변경

## 개요

- **문의하기 섹션**: 전화 카드 제거, 이메일 문의와 온라인 견적 요청만 표시
- **Footer**: 전화번호 항목 제거 및 주소 변경 (서울특별시 강남구 압구정로 306)

---

## 1. 전화 카드 제거

### 문의하기 섹션

- **제거 대상**: 전화 카드 (📞 Phone, +82-1644-0464, "Call Now →")
- **유지**: 이메일 문의, 온라인 견적 요청 2개만 표시
- **CSS 수정**: `.contact-grid-two` 클래스 추가하여 2개 카드 중앙 정렬
  - 데스크톱: 2열 그리드, `max-width: 720px`, 중앙 정렬
  - 모바일: 1열로 쌓임

### Footer 회사 정보

- **제거**: Footer의 `<strong>전화:</strong> +82-1644-0464` 항목 제거
- **대상 파일**: `src/html/index_v2*.html` (한/영/일/중 4개)

---

## 2. 주소 변경

### 변경 내용

- **이전**: 제주특별자치도 제주시 구남로7길 13-2 3층
- **변경**: 서울특별시 강남구 압구정로 306

### 언어별 표기

| 언어 | 주소 |
|------|------|
| 한국어 | 서울특별시 강남구 압구정로 306 |
| 영어 | 306, Apgujeong-ro, Gangnam-gu, Seoul |
| 일본어 | ソウル特別市江南区押鴎亭路306 |
| 중국어 | 首尔特别市江南区狎鸥亭路306 |

### 수정 파일

- `src/html/index_v2_ko.html` (한국어)
- `src/html/index_v2.html` (영어)
- `src/html/index_v2_ja.html` (일본어)
- `src/html/index_v2_zh.html` (중국어)

---

## 3. CSS 수정

### 파일: `src/css/styles_v2.css`

```css
/* 연락처 카드 2개일 때 중앙 정렬 */
.contact-grid-two {
    grid-template-columns: repeat(2, minmax(280px, 1fr));
    max-width: 720px;
    margin: 0 auto;
}

/* 모바일 반응형 */
@media (max-width: 768px) {
    .contact-grid-two {
        grid-template-columns: 1fr;
        max-width: none;
    }
}
```

---

## 4. 배포

### 빌드 및 배포

```bash
# 빌드
./scripts/deploy-cloudways.sh

# Cloudways SFTP 업로드
TOKEN=$(curl -s -X POST "https://api.cloudways.com/api/v1/oauth/access_token" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "email=cafedeers@gmail.com&api_key=i1R3cTx0zj1hJTHISNGNrkNS6b5IWd" \
  | python3 -c "import sys,json; print(json.load(sys.stdin).get('access_token',''))")

SERVER_DATA=$(curl -s -X GET "https://api.cloudways.com/api/v1/server" \
  -H "Authorization: Bearer $TOKEN")

MASTER_USER=$(echo "$SERVER_DATA" | python3 -c "import sys,json; s=json.load(sys.stdin)['servers'][0]; print(s['master_user'])")
MASTER_PASS=$(echo "$SERVER_DATA" | python3 -c "import sys,json; s=json.load(sys.stdin)['servers'][0]; print(s['master_password'])")

.venv_deploy/bin/python3 scripts/upload_sftp.py \
  68.183.180.33 "$MASTER_USER" "$MASTER_PASS" \
  dist applications/tjjsevvqfe/public_html
```

### 배포 서버

- **서버 IP**: 68.183.180.33
- **앱 경로**: `/applications/tjjsevvqfe/public_html`
- **임시 URL**: https://phpstack-1574100-6221145.cloudwaysapps.com/

---

## 5. 참고 경로/파일

| 구분 | 경로/파일 |
|------|-----------|
| 수정된 HTML | `src/html/index_v2*.html` (4개) |
| 수정된 CSS | `src/css/styles_v2.css` |
| 배포 스크립트 | `scripts/deploy-cloudways.sh` |
| SFTP 업로드 | `scripts/upload_sftp.py` |
| Cloudways 인증 | `dev/2026-02-18_Cloudways배포_Slack연동/CLOUDWAYS_인증정보.md` |

---

## 6. 확인 사항

- ✅ 문의하기 섹션: 전화 카드 제거 확인
- ✅ Footer: 전화번호 항목 제거 확인
- ✅ 주소: 모든 언어 버전에서 서울 강남구 압구정로 306으로 변경 확인
- ✅ CSS: 2개 카드 중앙 정렬 확인
- ✅ 배포: Cloudways 서버에 업로드 완료
